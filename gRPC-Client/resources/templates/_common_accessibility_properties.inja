{% if exists("accessible_name") %}
Accessible.name: "{{ accessible_name }}"
{% endif %}
{% if exists("accessible_description") %}
Accessible.description: "{{ accessible_description }}"
{% endif %}
{% if exists("accessible_role") %}
Accessible.role: {{ accessible_role }} // e.g. Accessible.Button, Accessible.Text, etc.
{% endif %}